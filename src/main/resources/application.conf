my-app {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 5s
  }
}

akka.persistence.journal.plugin = "akka.persistence.journal.leveldb"

akka.actor {
  serialization-bindings {
    "com.btrajkovski.JsonSerializable" = jackson-json
  }
}